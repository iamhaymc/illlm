{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "python",
            "args": [
                "util/make.py"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}